window.ENV = {
    // If you are deploying the frontend on Vercel and backend on Render, 
    // set this to your Render backend URL, e.g. "https://your-app.onrender.com/api"
    // If deploying full-stack on Render together, you can leave it as "/api"
    API_BASE_URL: "http://localhost:8000/api"
};
